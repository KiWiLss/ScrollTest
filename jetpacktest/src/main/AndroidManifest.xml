<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.kiwilss.jetpacktest">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <application
            android:name=".MyApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".life.LifeActivity"/>

        <activity android:name=".life.LifeCycleActivity"/>

        <activity android:name=".livedata.LiveDataActivity"/>

        <activity android:name=".viewmodel.ModelActivity"/>

        <activity android:name=".viewmodel.ModelFragment"/>

        <activity android:name=".paging.RvActivity"/>

        <activity android:name=".paging.jpaging.PagingActivity"/>

        <activity android:name=".utils.UtilsActivity"/>
    </application>

</manifest>